import {FormEvent,useState} from 'react';import {Navigate} from 'react-router-dom';import {ShieldCheck} from 'lucide-react';import {useAuth} from '../stores/auth';
export function LoginPage(){const auth=useAuth();const [username,setUsername]=useState('reviewer');const [password,setPassword]=useState('ProfileSearch2026!');const [error,setError]=useState('');const [loading,setLoading]=useState(false);if(auth.user)return <Navigate to="/search" replace/>;async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{await auth.login(username,password)}catch(e){setError(e instanceof Error?e.message:'ورود به سامانه امکان‌پذیر نیست.')}finally{setLoading(false)}}return <main className="login"><section className="login-copy"><div className="eyebrow">جست‌وجو و تحلیل پروفایل‌ها</div><h1>در میان تجربه‌ها،<br/>فرد مناسب را پیدا کنید.</h1><p>جست‌وجوی حرفه‌ای و رتبه‌بندی‌شده در مهارت‌ها، نقش‌ها و سوابق شغلی.</p><div className="trust"><ShieldCheck/>محیط خصوصی ارزیابی</div></section><form className="login-card" onSubmit={submit}><h2>خوش آمدید</h2><p>برای جست‌وجو در بانک استعدادها وارد شوید.</p><label>نام کاربری<input dir="ltr" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/></label><label>رمز عبور<input dir="ltr" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<div className="error" role="alert">{error}</div>}<button className="primary" disabled={loading}>{loading?'در حال ورود…':'ورود'}</button><button type="button" className="demo" onClick={()=>{setUsername('reviewer');setPassword('ProfileSearch2026!')}}>استفاده از حساب ارزیابی</button></form></main>}
