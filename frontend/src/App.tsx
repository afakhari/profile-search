import {useEffect} from 'react';import {Navigate,Route,Routes} from 'react-router-dom';import {Layout} from './components/Layout';import {DashboardPage} from './pages/DashboardPage';import {LoginPage} from './pages/LoginPage';import {ProfilePage} from './pages/ProfilePage';import {SearchPage} from './pages/SearchPage';import {useAuth} from './stores/auth';
function Protected(){const {user,ready,restore}=useAuth();useEffect(()=>{if(!ready)restore()},[ready,restore]);if(!ready)return <div className="boot">در حال راه‌اندازی سیگنال…</div>;return user?<Layout/>:<Navigate to="/login" replace/>}
export default function App(){return <Routes><Route path="/login" element={<LoginPage/>}/><Route element={<Protected/>}><Route path="/search" element={<SearchPage/>}/><Route path="/profiles/:id" element={<ProfilePage/>}/><Route path="/dashboard" element={<DashboardPage/>}/></Route><Route path="*" element={<Navigate to="/search" replace/>}/></Routes>}
